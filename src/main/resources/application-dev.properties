# Activated locally with SPRING_PROFILES_ACTIVE=dev (or spring.profiles.active=dev
# in application-local.properties). Re-enables Thymeleaf template hot-reload,
# which application.properties intentionally leaves off so production runs with
# templates cached.
spring.thymeleaf.cache=false
